module.exports = {
  arrowParens: 'avoid', // Avoids parentheses for single-parameter arrow functions
  bracketSameLine: true, // Keeps JSX tags’ closing brackets on the same line
  bracketSpacing: false, // Disables spacing between brackets in object literals
  singleQuote: true, // Uses single quotes instead of double quotes
  trailingComma: 'all', // Adds trailing commas wherever possible
  jsxSingleQuote: true, // Uses single quotes in JSX attributes
  printWidth: 100, // Adjusts max line width (optional for readability)
  semi: false, // Omits semicolons (optional for style)
};
